version: '3'

services:

#======= NGINX =======#
  nginx:
    init: true
    depends_on:
      - wordpress
      - mariadb
    build: ./requirements/nginx   # context ?
    ports:
      - 443:443
    container_name: nginx
    restart: always
    networks:
      - inception_network
    volumes:
      - php_nginx:/var/www/html

#===== WORDPRESS =====#
  wordpress:
    init: true
    depends_on:
      - mariadb
    build: ./requirements/wordpress  # context ?
    ports:
      - 9000:9000
    container_name: wordpress
    restart: always
    networks:
      - inception_network
    env_file:
      - .env
    volumes:
      - php_nginx:/var/www/html


#====== MARIADB ======#
  mariadb:
    init: true
    build: ./requirements/mariadb   # context ?
    ports:
      - 3306:3306
    container_name: mariadb
    restart: always
    networks:
      - inception_network
    env_file:
      - .env
    volumes:
      - database:/var/lib/mysql

# NETWORKS #

networks:
  inception_network:

# VOLUMES #

volumes:

  php_nginx:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/ade-la-c/data/php_nginx

  database:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/ade-la-c/data/db
